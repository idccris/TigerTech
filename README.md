# NOVA — Catálogo de produtos

Site de catálogo responsivo criado com Next.js para publicação na Vercel.

## Desenvolvimento

```bash
npm install
npm run dev
```

Antes de publicar, substitua o número provisório do WhatsApp em `components/catalog.tsx`.
